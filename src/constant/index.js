const AUTH_LITERAL = {
  PASSWORD: 'password',
  CODE: 'code',
};

const THEME = {
  LIGHT: 'light',
  DARK: 'dark',
};

const LANGUAGE = {
  ENGLISH: 'en',
  TAMIL: 'ta',
};

const constant = {
  APP_NAME: 'newor',
  AUTH_LITERAL,
  THEME,
  LANGUAGE,
};

export default constant;
