const AUTH_LITERAL = {
  PASSWORD: 'password',
  CODE: 'code',
};

const THEME = {
  LIGHT: 'light',
  DARK: 'dark',
};

const LANGUAGE = {
  ENGLISH: 'en',
  TAMIL: 'ta',
};

const STORAGE_KEY = {
  TOKEN: 'NEWOR_AUTH_TOKEN',
};

const constant = {
  APP_NAME: 'newor',
  AUTH_LITERAL,
  THEME,
  LANGUAGE,
  STORAGE_KEY,
};

export default constant;
