import React, { useState } from 'react';
import {
  ScrollView, VStack,
} from 'native-base';

const AddDeposit = () => {
  useState({});

  return (
    <ScrollView>
      <VStack p={5} />
    </ScrollView>
  );
};

export default AddDeposit;
